# Host ports (container still listens on 1025/8025 inside).
# If you get "Bind for 0.0.0.0:1025 failed: port is already allocated",
# either stop the other container using 1025 or use free ports below.
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025